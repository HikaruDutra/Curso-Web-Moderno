const [a, b, c, d] = [3, 5, 6, 15]

const soma = a + b + c + d
const subtracao = c - b
const multiplicacao = b * d
const divisao = d / a
const modulo = c % 2

console.log(soma, subtracao, multiplicacao, -divisao, modulo)